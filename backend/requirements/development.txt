# Development dependencies
#
# Pulls in production.txt (not just base.txt) because
# apps/core/test_production_settings_guard.py boots real production settings
# in a subprocess to verify they don't crash — that subprocess conditionally
# imports sentry_sdk (when SENTRY_DSN is set) and dj_database_url (when
# DATABASE_URL is set), both production-only packages. Picking those two
# individually already caused two rounds of "ModuleNotFoundError" in CI;
# pulling the whole production.txt avoids the next one-at-a-time surprise.
-r production.txt

# Testing
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
factory-boy==3.3.1
faker==33.3.1

# Debug tools
django-debug-toolbar==4.4.6
ipython==8.31.0
ipdb==0.13.13

# Code quality
flake8==7.1.1
black==24.10.0
isort==5.13.2
mypy==1.13.0
django-stubs==5.1.1
