# Cloud / production Tauri build — backend is your Railway deployment
# Replace the URL below with your actual Railway domain before building
VITE_API_BASE_URL=https://audity-backend-production-30f9.up.railway.app/api/v1

# ── PostHog product analytics (monitoring) ──────────────────────────────────
# Paste your PostHog project API key below to enable analytics in cloud builds.
# Find it in PostHog → Settings → Project → Project API Key (starts with "phc_").
# Leave blank to disable analytics entirely (the app no-ops gracefully).
VITE_POSTHOG_KEY=phc_zfzihL4so9zEFpKcEGkVMiY4g6PJ8vwiAAuPS4Xd99Q3
# US cloud: https://us.i.posthog.com  |  EU cloud: https://eu.i.posthog.com
VITE_POSTHOG_HOST=https://us.i.posthog.com
