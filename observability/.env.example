# Copy to .env and adjust. Used by docker-compose.yml in this folder.

# ── Postgres connection (must match your backend .env / main docker-compose) ──
DB_NAME=finventory
DB_USER=finv_app
DB_PASSWORD=postgres

# ── Grafana admin login ──
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=admin
