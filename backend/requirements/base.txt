# ============================================================
# BASE REQUIREMENTS - All environments
# ============================================================

# Core Django
Django==5.1.15
djangorestframework==3.15.2
django-cors-headers==4.6.0

# Authentication
djangorestframework-simplejwt==5.5.1

# Database
psycopg2-binary==2.9.10

# Caching & Async
redis==5.2.1
django-redis==5.4.0
celery==5.4.0
django-celery-beat==2.7.0
kombu==5.4.2

# Filtering & Search
django-filter==24.3
drf-spectacular==0.28.0

# HTTP client (Paystack API calls)
requests==2.33.0

# Transactional email via HTTP API (bypasses SMTP port restrictions on Railway)
django-anymail[brevo]==12.0

# Utilities
python-decouple==3.8
pillow==12.1.1
python-dateutil==2.9.0
pytz==2024.2

# Reporting
reportlab==4.2.5
openpyxl==3.1.5

# Security
cryptography==46.0.6
django-ratelimit==4.1.0
argon2-cffi==23.1.0
pyotp==2.9.0
qrcode[pil]==8.0

# Logging
structlog==24.4.0

# File type detection — magic byte sniffing for upload validation (pure Python, no libmagic needed)
filetype==1.2.0

# AI bank reconciliation — Groq free tier (same key used by AI Financial Assistant)
groq>=0.8.0

# DNS verification for white-label domain ownership checks
dnspython==2.7.0

# Analytics
posthog>=3.0.0
